# 

### 运行项目前

```javascript
npm i
```

### 复制模板，快速创建页面、组件

```javascript
npm run cli
```

## 目录
├── src
│   └── api                 // 接口
│   └── assets              // 资源文件
│   └── cli                 // 命令行构建页面、组件
│   └── components          // 主包和分包都使用的组件放到这里
│   └── config              // 环境、常量等公共配置
│   └── pages               // 主包相关的页面
│   └── styles              // 公共样式、字体库
│   └── subPackages         // 分包目录
│   │   └── packageOrder    // 订单
│   │   │   └── components  // 分包组件
│   │   │   └── common      // 分包公共方法
│   │   └── packageProduct  // 设备
│   │   └── packageMine     // 我的
│   └── utils               // 埋点、公共方法
│   └── app.js              // 入口逻辑
│   └── app.json            // 公共配置
│   └── app.less            // 公共样式表
├── eslint                  // eslint风格 eslint-airbnb
├── project.config.json     // 项目配置文件